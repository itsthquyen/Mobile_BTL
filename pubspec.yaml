name: moblie_btl
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4
  shared_preferences: ^2.2.2
  smooth_page_indicator: ^1.1.0
  intl: ^0.19.0
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  collection: ^1.18.0 # Thêm dòng này

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
